name: Export local Android build kit

on:
  push:
    branches:
      - fix/3.0.1-manual-test-bugs
    paths:
      - .github/workflows/export-local-android-build-kit.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout exact head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
      - name: Set up JDK 17
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb
      - name: Setup validation google-services.json
        shell: bash
        run: |
          set -euo pipefail
          cat << 'EOF' > app/google-services.json
          {
            "project_info": {"project_number":"1234567890","project_id":"dummy-project","storage_bucket":"dummy-project.appspot.com"},
            "client": [{"client_info":{"mobilesdk_app_id":"1:1234567890:android:1234567890abcdef","android_client_info":{"package_name":"com.aistudio.brasfutretro.djuxzt"}},"oauth_client":[],"api_key":[{"current_key":"AIzaSyDummyKeyForBuildOnly123456"}],"services":{"appinvite_service":{"status":1}}}],
            "configuration_version":"1"
          }
          EOF
      - name: Setup Debug Keystore
        shell: bash
        run: |
          set -euo pipefail
          keytool -genkey -v -keystore debug.keystore \
            -storepass android -alias androiddebugkey -keypass android \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug, O=Android, C=US"
          mkdir -p ~/.android
          cp debug.keystore ~/.android/debug.keystore
      - name: Warm exact Android/Gradle dependencies
        shell: bash
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew assembleDebug -PexcludeStressTests=true --stacktrace
      - name: Package and split offline build kit
        shell: bash
        run: |
          set -euo pipefail
          echo "ANDROID_HOME=$ANDROID_HOME"
          du -sh "$HOME/.gradle/caches" "$ANDROID_HOME/platforms/android-35" "$ANDROID_HOME/build-tools" || true
          mkdir -p offline-kit-parts
          tar --zstd -cf "$RUNNER_TEMP/android-gradle-build-kit.tar.zst" \
            -C "$HOME" .gradle/caches \
            -C "$ANDROID_HOME" platforms/android-35 build-tools platform-tools licenses
          sha256sum "$RUNNER_TEMP/android-gradle-build-kit.tar.zst" | tee offline-kit-parts/SHA256SUMS.txt
          printf 'head=%s\nandroid_home=%s\n' "$GITHUB_SHA" "$ANDROID_HOME" > offline-kit-parts/BUILD_KIT_INFO.txt
          split -b 240M -d -a 2 "$RUNNER_TEMP/android-gradle-build-kit.tar.zst" offline-kit-parts/part-
          test "$(find offline-kit-parts -maxdepth 1 -type f -name 'part-*' | wc -l)" -eq 6
          ls -lh offline-kit-parts
      - name: Upload offline kit part 00
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: android-build-kit-part-00-${{ github.sha }}
          path: offline-kit-parts/part-00
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
      - name: Upload offline kit part 01
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: android-build-kit-part-01-${{ github.sha }}
          path: offline-kit-parts/part-01
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
      - name: Upload offline kit part 02
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: android-build-kit-part-02-${{ github.sha }}
          path: offline-kit-parts/part-02
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
      - name: Upload offline kit part 03
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: android-build-kit-part-03-${{ github.sha }}
          path: offline-kit-parts/part-03
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
      - name: Upload offline kit part 04
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: android-build-kit-part-04-${{ github.sha }}
          path: offline-kit-parts/part-04
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
      - name: Upload offline kit part 05 and metadata
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: android-build-kit-part-05-${{ github.sha }}
          path: |
            offline-kit-parts/part-05
            offline-kit-parts/SHA256SUMS.txt
            offline-kit-parts/BUILD_KIT_INFO.txt
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
